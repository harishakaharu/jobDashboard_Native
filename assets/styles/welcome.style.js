import { StyleSheet } from "react-native";

export const welcomeStyle = StyleSheet.create({
  searchIcon: {},
  searchContainer: { flexDirection: "row", alignItems: "center" },
  txtContainer: { width: 300 },
});
